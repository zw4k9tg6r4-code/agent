{
  "name": "agent-runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w @agent/contracts && npm run build -w @agent/tools && npm run build -w @agent/policy && npm run build -w @agent/core && npm run build -w @agent/providers && npm run build -w @agent/cli",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "verify": "npm run build && npm run typecheck && npm run test:coverage"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
